<% provide(:title, "Propebis | Recuperar Contraseña") %>

<div class="passwordBox animated fadeInDown">
  <div class="row">
    <div class="col-md-12">
      <div class="ibox-content">
        <h2 class="font-bold">¿Olvidaste tu contraseña?</h2>
        <p>Ingresa tu correo electrónico y te enviaremos tu contraseña reestablecida a tu correo.</p>
        <div class="row">
          <div class="col-lg-12">
            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
              <%= devise_error_messages! %>

              <div class="form-group">
                <%= f.label :email %><br />
                <%= f.email_field :email, class: "form-control", placeholder: 'e-mail', autofocus: true %>
              </div>

              <div class="actions">
                <%= f.submit "Enviar Nueva Contraseña", class: 'btn btn-primary block full-width m-b' %>
              </div>
            <% end %>

          </div>
        </div>
      </div>
    </div>
  </div>
  <hr />
  <div class="row">
    <div class="col-md-6">
      Copyright Example Company
    </div>
    <div class="col-md-6 text-right">
      <small>© 2014-2015</small>
    </div>
  </div>
</div>
