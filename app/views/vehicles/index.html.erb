<% provide(:title, "Propebis | Vehículos") %>
<%= render 'shared/notifications' %>

<div class="row wrapper border-bottom white-bg page-heading breadcrumbs-margin">
  <div class="col-lg-10">
    <h2>Vehículos</h2>
    <ol class="breadcrumb">
      <li>
        <a href="<%= url_for :controller => 'static_pages', :action => 'home' %>">Home</a>
      </li>
      <li class="active">
        <strong>Vehículos</strong>
      </li>
    </ol>
  </div>
  <div class="col-lg-2">
  </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h5>Vehículos</h5>
        </div>
        <div class="ibox-content">
          <%= button_to 'Nuevo Vehículo', { action: 'new'}, method: :get, class: 'btn btn-w-m btn-primary' %>
          <input type="text" class="form-control input-sm m-b-xs" id="filter" placeholder="Buscar">
          <div class='table-responsive'>
            <table class="table table-stripped table-bordered table-hover datatable">
              <thead>
                <tr>
                  <th align='center'>ID</th>
                  <th align='center'>Placa</th>
                  <th align='center'>Marca</th>
                  <th align='center'>Capacidad</th>
                  <th align='center'>Editar</th>
                  <th align='center'>Eliminar</th>
                </tr>
              </thead>
              <tbody>
                <% @vehicles.each do |vehicle| %>
                  <tr>
                    <td align='center'><%= vehicle.id %></td>
                    <td align='center'><%= vehicle.plate %></td>
                    <td align='center'><%= vehicle.trademark %></td>
                    <td align='center'><%= vehicle.capacity %></td>
                    <td align='center'><%= link_to 'Editar', { action: 'edit', id: vehicle.id}, class: 'btn btn-w-m btn-warning btn-xs' %></td>
                    <td align='center'><%= link_to 'Eliminar', { action: 'destroy', id: vehicle.id}, method: :delete,  data: { confirm: '¿Está seguro?' }, class: 'btn btn-w-m btn-danger btn-xs' %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
        <!--Fin Ibox Content-->
      </div>
      <!--Fin Ibox-->
    </div>
  </div>
</div>

<% content_for :javascript do %>
  <script type="text/javascript">
    $(function() {
      load_table_controls('datatable');
    });
  </script>
<% end %>