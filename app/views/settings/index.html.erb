<% provide(:title, "Propebis | Configuración") %>
<div class="row wrapper border-bottom white-bg page-heading breadcrumbs-margin">
  <div class="col-lg-12">
    <h2>Configuración</h2>
    <ol class="breadcrumb">
      <li>
        <a href="<%= url_for :controller => 'static_pages', :action => 'home' %>">Inicio</a>
      </li>
      <li class="active">
        <strong>Configuración</strong>
      </li>
    </ol>
  </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight no-padding">
  <%= render 'shared/notifications' %>
  <div class="row breadcrumbs-margin">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h5>Configuración</h5>
        </div>
        <div class="ibox-content">
          <div class='table-responsive'>
            <table class="table table-stripped table-bordered table-hover datatable">
              <thead>
                <tr>
                  <th align='center'>Alerta de Expiración</th>
                  <th align='center'>Editar</th>
                </tr>
              </thead>
              <tbody>
                <% @settings.each do |setting| %>
                  <tr>
                    <td><%= setting.expiration_alert %></td>
                    <td class="center"><%= link_to 'Editar', { action: 'edit', id: setting.id}, class: 'btn btn-w-m btn-warning btn-xs' %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>  
        </div>
        <!--Fin Ibox Content-->
      </div>
      <!--Fin Ibox-->
    </div>
  </div>
</div>

<% content_for :javascript do %>
  <script type="text/javascript">
    $(function() {
      load_table_controls('datatable','Configuración');
    });
  </script>
<% end %>