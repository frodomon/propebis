<% provide(:title, "Propebis | Clientes") %>
<%= render 'shared/notifications' %>

<div class="row wrapper border-bottom white-bg page-heading breadcrumbs-margin">
  <div class="col-lg-10">
    <h2>Clientes</h2>
    <ol class="breadcrumb">
      <li>
        <a href="<%= url_for :controller => 'static_pages', :action => 'home' %>">Home</a>
      </li>
      <li class="active">
        <strong>Clientes</strong>
      </li>
    </ol>
  </div>
  <div class="col-lg-2">
  </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h5>Clientes</h5>
        </div>
        <div class="ibox-content">
          <%= button_to 'Nuevo Cliente', { action: 'new'}, method: :get, class: 'btn btn-w-m btn-primary' %>
          <input type="text" class="form-control input-sm m-b-xs" id="filter" placeholder="Buscar">
          <table class="footable table table-stripped toggle-arrow-tiny" data-page-size="25" data-filter=#filter>
            <thead>
              <tr>
                <th>Razón Social</th>
                <th>RUC</th>
                <th>Dirección de Facturación</th>
                <th>Dirección de Entrega</th>
                <th>Teléfono</th>
                <th>Contacto</th>
                <th>Activo</th>
                <th colspan="2"></th>
              </tr>
            </thead>
            <tbody>
              <% @clients.each do |client| %>
                <tr>
                  <td class="center"><%= client.name %></td>
                  <td class="center"><%= client.ruc %></td>
                  <td class="center"><%= client.billing_address %></td>
                  <td class="center"><%= client.delivery_address %></td>
                  <td class="center"><%= client.telephone %></td>
                  <td class="center"><%= client.contact %></td>
                  <td class="center"><%= human_boolean(client.active) %></td>
                  <td class="center"><%= button_to 'Editar', { action: 'edit', id: client.id}, method: :get, class: 'btn btn-w-m btn-warning btn-xs' %></td>
                  <td class="center"><%= button_to 'Eliminar', { action: 'destroy', id: client.id}, method: :delete,  data: { confirm: '¿Está seguro?' }, class: 'btn btn-w-m btn-danger btn-xs' %></td>
                </tr>
              <% end %>
            </tbody>
            <tfoot>
              <tr>
                <td colspan='5'>
                  <ul class="pagination pull-right"></ul>
                </td>
              </tr>
            </tfoot>
          </table>
          
        </div>
        <!--Fin Ibox Content-->
      </div>
      <!--Fin Ibox-->
    </div>
  </div>
</div>

<% content_for :javascript do %>
  <script type="text/javascript">
    $(function() {
      $('.footable').footable();
      $('.footable2').footable();
    });
  </script>
<% end %>